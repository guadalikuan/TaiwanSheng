# 网络配置系统更新说明

## ✅ 已完成的更改

### 1. 创建统一配置文件

**文件**: `tws/solana.config.js`

这是核心配置文件，所有网络相关的设置都在这里：

```javascript
CLUSTER: 'mainnet-beta',  // 默认主网，修改这里切换网络
```

### 2. 创建配置管理脚本

**文件**: `tws/scripts/setup-solana-network.js`

自动更新 `Anchor.toml` 并显示当前配置信息。

**文件**: `tws/scripts/switch-network.js`

跨平台网络切换脚本，支持 Windows/Linux/Mac。

### 3. 更新所有相关脚本

以下文件已更新为使用统一配置：

- ✅ `scripts/deploy-solana.js` - 部署脚本
- ✅ `scripts/initialize-bunker.js` - 初始化脚本
- ✅ `server/utils/solanaBlockchain.js` - 后端服务
- ✅ `Anchor.toml` - 添加了说明注释

### 4. 更新 package.json

新增的命令：

```json
{
  "solana:config": "显示并更新配置",
  "solana:switch:mainnet": "快速切换到主网",
  "solana:switch:devnet": "快速切换到测试网",
  "solana:deploy": "使用当前配置部署",
  "solana:deploy:devnet": "强制部署到测试网",
  "solana:deploy:mainnet": "强制部署到主网"
}
```

## 🎯 使用方法

### 切换到主网（推荐方式）

1. **编辑配置文件** `tws/solana.config.js`：
   ```javascript
   CLUSTER: 'mainnet-beta',  // 确保这里是 mainnet-beta
   ```

2. **运行配置脚本**：
   ```bash
   npm run solana:config
   ```

3. **部署**：
   ```bash
   npm run solana:deploy
   ```

### 快速切换命令

```bash
# 切换到主网
npm run solana:switch:mainnet

# 切换到测试网
npm run solana:switch:devnet
```

## 📋 配置优先级

配置读取优先级（从高到低）：

1. 环境变量 `SOLANA_CLUSTER`
2. `solana.config.js` 中的 `CLUSTER` 值
3. 默认值 `'mainnet-beta'`

## 🔍 验证配置

运行以下命令查看当前配置：

```bash
npm run solana:config
```

## ⚠️ 重要提示

1. **默认配置**: 现在默认设置为 `mainnet-beta`（主网）
2. **测试建议**: 首次部署建议先在 devnet 测试
3. **主网警告**: 部署到主网需要真实 SOL，且不可撤销

## 📚 相关文档

- [网络配置指南](./SOLANA_NETWORK_CONFIG.md) - 详细使用说明
- [部署文档](./SOLANA_DEPLOYMENT.md) - 完整部署流程
- [快速开始](./SOLANA_QUICKSTART.md) - 快速上手指南

## 🔄 迁移说明

如果您之前使用的是 devnet，现在需要：

1. 确认要使用的网络（devnet 或 mainnet-beta）
2. 运行相应的切换命令
3. 重新部署程序

所有脚本和配置已自动同步，无需手动修改多个文件。



