{
  "name": "tot-token",
  "version": "0.1.0",
  "description": "TaiOneToken (TOT) - Solana SPL Token-2022 with dynamic tax system",
  "main": "index.js",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "test:report": "anchor test --skip-local-validator && npm run test:generate-report",
    "test:generate-report": "mochawesome-merge ../tests/reports/mochawesome/*.json -o ../tests/reports/report.json 2>/dev/null || echo 'No test results to merge' && mochawesome-report-generator ../tests/reports/report.json -o ../tests/reports 2>/dev/null || echo 'Report generation skipped'",
    "test:full": "npm run test && npm run test:generate-report",
    "deploy": "ts-node ../client/deploy.ts",
    "initialize": "ts-node ../client/initialize.ts",
    "mint": "ts-node ../client/mint_to_pools.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/web3.js": "^1.87.6",
    "@solana/spl-token": "^0.3.9"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
